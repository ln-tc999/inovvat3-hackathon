---
import WalletConnect from "../../islands/WalletConnect";
---

<section id="cta" class="lp-section lp-divider border-t min-h-screen flex flex-col justify-center px-4 sm:px-6 lg:px-8 py-16 sm:py-20">
  <div class="max-w-3xl mx-auto w-full text-center">

    <p class="lp-muted text-xs uppercase tracking-widest mb-5 sm:mb-6">Get started</p>
    <h2 class="lp-text font-black mb-5 sm:mb-6 leading-tight" style="font-size: clamp(2rem, 5vw, 3.5rem);">
      Set It Once.<br/>
      <span class="lp-accent">Earn Forever.</span>
    </h2>
    <p class="lp-muted mb-8 sm:mb-10 max-w-lg mx-auto leading-relaxed"
      style="font-size: clamp(0.875rem, 1.25vw, 1rem);">
      Connect your wallet, complete onboarding in under 2 minutes, and let
      Vatiin AI handle the rest. No DeFi expertise required.
    </p>

    <div class="flex flex-col sm:flex-row items-center justify-center gap-3 sm:gap-4">
      <WalletConnect client:load variant="hero" />
      <a href="/dashboard"
        class="btn-fill-up lp-badge px-6 py-3 text-sm font-medium">
        Open Dashboard
      </a>
    </div>

    <!-- ecosystem pills -->
    <div class="mt-12 sm:mt-14 flex flex-wrap items-center justify-center gap-2 sm:gap-3">
      {[
        "Aave V3",
        "Morpho Blue",
        "Chainlink Automation",
        "Qwen 3 235B",
        "Base Chain",
        "RainbowKit",
        "Vercel + Railway",
      ].map((name) => (
        <span class="lp-tag">{name}</span>
      ))}
    </div>

  </div>
</section>
